import styled from "styled-components";

export const Container = styled.div`
  position: sticky;
  top: 0;
  height: 100vh;
  margin-top: -100vh;

  display: flex;
  align-items: column;
  justify-content: center;
`;
